apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "runsafe.fullname" . }}-test-connection"
  labels:
    {{- include "runsafe.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "runsafe.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
