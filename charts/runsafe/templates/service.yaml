apiVersion: v1
kind: Service
metadata:
  name: {{ include "runsafe.fullname" . }}
  labels:
    {{- include "runsafe.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "runsafe.selectorLabels" . | nindent 4 }}
